name: Build APK
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Build APK
        uses: fire1ce/buildozer-action@v1
        with:
          command: buildozer android debug
          workdir: ./
